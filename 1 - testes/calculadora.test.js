const { somar, multiplicar } = require('./calculadora.js')

describe('testes da função somar', () => {

    test('deve retornar 4 quando somar 2 e 2', () => {
        // implementação do teste
    
        const result = {}
    
        expect(result).toEqual({})
    })

})
